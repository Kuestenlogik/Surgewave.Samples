<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Uses Surgewave's Confluent.Kafka wrapper - provides same namespace as Confluent.Kafka -->
    <PackageReference Include="Kuestenlogik.Surgewave.Compatibility.Confluent.Kafka" />
  </ItemGroup>

</Project>
